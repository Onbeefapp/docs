required: true
content:
  application/json:
    schema:
      $ref: '#/components/schemas/TokenRequest'
    examples:
      tokenRequestExemple:
        $ref: '#/components/examples/TokenRequestExample'